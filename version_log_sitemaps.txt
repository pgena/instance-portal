Карта Сайта v2.1
--------------------------------------------------------------------------------
[+] Добавлена постраничная генерация карт если материалов больше 50000;
[+] Добавлена настройка полной перегенерации карт если их несколько страниц;
[=] Исправлена ошибка с показом скрытых материалов (например фотографий пользователей где стояло ограничение доступа);
[=] Теперь параметр время повторной генерации последней страницы карты действует и при входе на страницу компонента так и по CRON;
[=] Изменен формат подключаемых файлов компонентов для генерации;
    1. Подключаемые классы должны наследовать основной абстрактный класс comMaps;
    2. В подключаемом классе обязательно должны быть реализованы следующие функции:
        public function FillMapsArray($html=FALSE){
            //Обязательная функция!!!
        }
        public function user_map_start(){
            //Обязательно должна быть в классе но если не используется то должна возвращать FALSE
            return FALSE;
        }
        public function user_re_map_start(){
            //Обязательно должна быть в классе но если не используется то должна возвращать FALSE
            return FALSE;
        }
        public function user_set_map_url(){
            //Обязательно должна быть в классе но если не используется то должна возвращать FALSE
            return FALSE;
        }
        public function user_map_end(){
            //Обязательно должна быть в классе но если не используется то должна возвращать FALSE
            return FALSE;
        }
        public function user_genMapsList(){
            //Обязательно должна быть в классе но если не используется то должна возвращать FALSE
            return FALSE;
        }
    и переменные:
        public $title = "Название компонента, или любой другой текст для вывода в html карте на сайте";
        public $link = "Название файлов карт например moy-komponent";
    так же в обязательном плане в __construct нужно инициализировать класс базы данных: $this->inDB = cmsDatabase::getInstance(); и присвоить переменной $this->total общее число материалов данного компонента; и после присваивания $this->total вызвать метод $this->map_start();
[=] Исправлен метод получения HOST при генерации по CRON, сделано получение системным методом;